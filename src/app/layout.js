import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Sistem Kasir Modern - Kelola Transaksi dengan Mudah & Cepat",
  description: "Sistem kasir terintegrasi untuk meningkatkan efisiensi bisnis Anda. Transaksi lebih cepat, laporan real-time, dan manajemen inventori yang mudah.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
